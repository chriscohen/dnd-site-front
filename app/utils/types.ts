export type BottomNavItem = {
    anchor: string
    disabled?: boolean
    icon: string
    name: string
};

export type EditionTabData = {
    id: string;
    class: string,
    label: string;
    suffix?: string
    disabled: boolean,
}
