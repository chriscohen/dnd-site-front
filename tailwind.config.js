/** @type {import('tailwindcss').Config} */
module.exports = {
    // Nuxt automatically configures the 'content' option, so you can leave it empty
    // unless you have weird file locations.
    content: [],
    theme: {
        extend: {},
    },
    plugins: [],
}
